# Copy this file to ".env" at the root of the react_frontend container and adjust values as needed.
# The app will use REACT_APP_BEAR_API_URL when present; otherwise it falls back to the default in code.
# Using this ensures the configuration persists across restarts.

REACT_APP_BEAR_API_URL=https://vscode-internal-14781-beta.beta01.cloud.kavia.ai:3001/api/bears

# Optional: override refresh interval (seconds). Defaults to 10 if not set.
# REACT_APP_REFRESH_INTERVAL_SECONDS=10
