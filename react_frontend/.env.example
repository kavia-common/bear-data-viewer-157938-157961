# React app environment configuration for Bear Pose Monitor
# Copy this file to ".env" at the same folder level and adjust values as needed.
# Note: In Create React App, environment variables must be prefixed with REACT_APP_

# Full API endpoint to fetch bear data
REACT_APP_BEAR_API_URL=http://localhost:5000/api/bears

# Auto-refresh interval (in seconds). Default is 10 if not set.
REACT_APP_REFRESH_INTERVAL_SECONDS=10
